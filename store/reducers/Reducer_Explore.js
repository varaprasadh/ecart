
const initialState = {};

//loaderstate on explore screen,
//load products
//load categories
//load more products


export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
